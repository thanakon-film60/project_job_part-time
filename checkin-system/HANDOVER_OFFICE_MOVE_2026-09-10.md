# ย้ายบริษัท + เพิ่มการแจ้งเตือน "สาย / ตรงเวลา" — สรุปให้ฝั่ง Production

วันที่: 10 กันยายน 2026
สาขา: `Film_dev_Part-Time`

---

## 1. สรุปสั้นๆ ว่าเปลี่ยนอะไร

1. **ย้ายสถานที่ทำงาน** — เหลือสาขาเดียวคือ `Motta & Montipa (Head office)`
   สถานที่เดิมทั้งหมด (MARDODI, BJH Bangkok, ถึงบ้านแล้ว) ถูกเอาออก
2. **เพิ่มเวลาทำงานมาตรฐาน** — เข้า 08:30 น. ออก 17:30 น.
3. **เพิ่มการแจ้งเตือนสาย/ตรงเวลา** — ทั้งในแอป (ตอนกดลงเวลา + การ์ดสรุปวันนี้)
   และในข้อความที่ส่งเข้ากลุ่ม LINE

> ⚠️ **สิ่งที่ production ต้องทำ อยู่ในหัวข้อ 3** — ถ้าไม่ตั้ง env ตามนั้น
> ระบบจะยังใช้ค่า default ในโค้ด (ซึ่งถูกต้องอยู่แล้ว) แต่จะแก้เวลา/พิกัด
> ในอนาคตไม่ได้โดยไม่ build ใหม่

---

## 2. พิกัดใหม่

| รายการ | ค่า |
| --- | --- |
| ชื่อสถานที่ | `Motta & Montipa (Head office)` |
| Latitude | `13.9040518` |
| Longitude | `100.5391995` |
| รัศมี geofence | `0.5` กม. (500 เมตร) |
| ประเภท (category) | `work` |
| ที่มา | https://maps.app.goo.gl/pnmwajnaByj1UBMn8 |

**หมายเหตุเรื่องรัศมี:** ของเดิม (MARDODI) ตั้งไว้ 2.0 กม. ซึ่งกว้างมาก —
ยืนอยู่นอกตึกไปสองกิโลก็ยังเช็คอินผ่าน ผมตั้งค่าใหม่เป็น **0.5 กม.**
ให้รัดกุมขึ้นแต่ยังเผื่อ GPS เพี้ยนในเมือง
👉 **ถ้าพนักงานเช็คอินไม่ผ่านบ่อย ให้ขยับเป็น `1.0` ก่อน** แก้ที่ `OFFICES` อย่างเดียว ไม่ต้อง build แอป

**ที่อยู่แบบตัวอักษร:** ยังไม่ได้ใส่ เพราะดึงจากหน้า Google Maps ไม่ได้
(หน้าเว็บ render ด้วย JS) — ไม่อยากกรอกมั่ว ถ้าต้องใช้ที่อยู่เต็มในเอกสาร
ให้ก๊อปจากหน้า Google Maps มาเติมที่ฟิลด์ `address` เอง

---

## 3. ✅ สิ่งที่ Production ต้องอัปเดต

### 3.1 ตั้งค่าใน `.env` ของ backend

เพิ่ม/แก้บรรทัดเหล่านี้ (ดูตัวอย่างเต็มใน `checkin-system/.env.example`):

```env
# --- สถานที่ทำงาน (JSON บรรทัดเดียว) ---
OFFICES=[{"name":"Motta & Montipa (Head office)","lat":13.9040518,"lng":100.5391995,"radius_km":0.5,"category":"work"}]

# --- เวลาทำงาน (เวลาไทย) ใช้ตัดสิน สาย / ออกก่อน ---
WORK_START_TIME=08:30
WORK_END_TIME=17:30
LATE_GRACE_MINUTES=0
EARLY_LEAVE_GRACE_MINUTES=0
ATTENDANCE_RULES_ENABLED=true
```

| ตัวแปร | ความหมาย | ค่าที่ตั้งไว้ |
| --- | --- | --- |
| `WORK_START_TIME` | เวลาเข้างานมาตรฐาน | `08:30` |
| `WORK_END_TIME` | เวลาออกงานมาตรฐาน | `17:30` |
| `LATE_GRACE_MINUTES` | ผ่อนผันกี่นาทีถึงเริ่มนับว่าสาย | `0` = เข้า 08:31 สายทันที |
| `EARLY_LEAVE_GRACE_MINUTES` | ออกก่อนเกินกี่นาทีถึงเตือน | `0` |
| `ATTENDANCE_RULES_ENABLED` | สวิตช์ปิดฟีเจอร์ทั้งระบบ | `true` |

> ⚠️ ไฟล์ `.env` ต้องเป็น **UTF-8 ไม่มี BOM** (ชื่อสถานที่มีอักขระ `&`)
> และ `OFFICES` ต้องอยู่ **บรรทัดเดียว** ห้ามตัดบรรทัด

**ถ้าอยากให้ผ่อนผัน 15 นาที** (เข้าถึง 08:45 ยังไม่ถือว่าสาย)
ตั้ง `LATE_GRACE_MINUTES=15` — ตัวเลขนาทีที่รายงานยังนับจาก 08:30 เหมือนเดิม
เพราะนั่นคือเลขที่ HR ใช้

### 3.2 รีสตาร์ท backend

ค่าพวกนี้อ่านตอน process เริ่มเท่านั้น แก้ `.env` แล้วต้องรีสตาร์ท

### 3.3 ตรวจว่าขึ้นจริง

```bash
curl https://thanakronpart-time.com/reports/geofence
```

ต้องเห็น:

```json
{
  "offices": [
    {
      "name": "Motta & Montipa (Head office)",
      "lat": 13.9040518,
      "lng": 100.5391995,
      "radius_km": 0.5,
      "allow_checkout": true,
      "category": "work"
    }
  ],
  "work_schedule": {
    "work_start": "08:30",
    "work_end": "17:30",
    "late_grace_minutes": 0,
    "early_leave_grace_minutes": 0,
    "enabled": true
  },
  "office_name": "Motta & Montipa (Head office)",
  "office_lat": 13.9040518,
  "office_lng": 100.5391995,
  "radius_km": 0.5
}
```

### 3.4 แจกแอปเวอร์ชันใหม่

แอปดึงสถานที่ + เวลาทำงานจาก `/reports/geofence` อยู่แล้ว
**ตั้ง `.env` อย่างเดียวแอปเก่าก็ย้ายพิกัดตาม** แต่จะยังไม่มีป้าย "สาย/ตรงเวลา"
บนหน้าจอ (ต้องเป็นแอปเวอร์ชันใหม่)

### 3.5 ข้อมูลเก่าใน DB

ประวัติเช็คอินเดิมยังเก็บ `office_name` เป็นชื่อเก่า (`MARDODI` ฯลฯ) —
**ตั้งใจไม่แตะ** เพราะเป็นบันทึกว่าตอนนั้นอยู่ที่ไหนจริงๆ
หน้าประวัติจะแสดงชื่อเก่าตามเดิม ไม่ถือว่าผิด

---

## 4. ตรรกะการตัดสิน "สาย / ตรงเวลา"

### 4.1 ตอนเข้างาน (`kind = "in"`)

| เวลาที่กด | ผล | ข้อความ |
| --- | --- | --- |
| 08:15 | ตรงเวลา | `ตรงเวลา (ก่อนเวลา 15 นาที)` |
| 08:30 | ตรงเวลา | `ตรงเวลา` |
| 08:31 | **สาย** | `สาย 1 นาที` |
| 10:05 | **สาย** | `สาย 1 ชม. 35 นาที` |

- เส้นตัดสิน = `WORK_START_TIME + LATE_GRACE_MINUTES`
- **จำนวนนาทีที่รายงานนับจาก 08:30 เสมอ** ไม่ใช่นับจากเส้นผ่อนผัน
  (ผ่อนผัน 15 นาที แล้วเข้า 08:46 → รายงาน "สาย 16 นาที")

### 4.2 ตอนออกงาน (`kind = "out"`)

| เวลาที่กด | ผล | ข้อความ |
| --- | --- | --- |
| 16:30 | **ออกก่อนเวลา** | `ออกก่อนเวลา 1 ชม.` |
| 17:30 | ครบเวลา | `ครบเวลางาน` |
| 18:45 | ครบเวลา | `ครบเวลางาน (เกินเวลา 1 ชม. 15 นาที)` |

### 4.3 กรณีที่ "ไม่ตัดสิน"

- ลงเวลาที่ **บ้าน** (category `home`) — อยู่บ้าน = ไม่ได้ไปทำงาน
  ตรรกะเดิมของระบบอยู่แล้ว ไม่มีสาย/ไม่มีออกก่อน
  *(ตอนนี้ไม่มีพิกัดบ้านในระบบแล้ว แต่โค้ดยังรองรับถ้าเพิ่มกลับมา)*
- ตั้ง `ATTENDANCE_RULES_ENABLED=false`
- `kind` ไม่ใช่ `in`/`out`

### 4.4 ข้อจำกัดที่รู้อยู่ (ตั้งใจ)

- **เทียบเฉพาะ "เวลาในวัน" ไม่ดูวันที่** — ระบบนี้เป็นกะกลางวัน
  ลงเวลาตอนตี 1 จะนับว่า "มาก่อนเวลา" ไม่ใช่สายข้ามวัน
  ถ้าอนาคตมีกะดึกต้องออกแบบใหม่
- **ไม่ได้เก็บสถานะสายลง DB** — คำนวณสดจากเวลาที่บันทึกไว้ทุกครั้ง
  ข้อดีคือแก้เกณฑ์ย้อนหลังแล้วรายงานเก่าถูกต้องตาม เกณฑ์ใหม่ทันที
  ถ้า HR ต้องการล็อกสถานะ ณ วันนั้น ต้องเพิ่มคอลัมน์ใน `checkins` (งานคนละก้อน)
- **ไม่รู้จักวันหยุด/วันลา** — เสาร์อาทิตย์กดเข้างาน 10 โมงก็ขึ้นว่าสาย
  ยังไม่มีตารางวันหยุดในระบบ

---

## 5. ไฟล์ที่แก้

### Backend

| ไฟล์ | สิ่งที่ทำ |
| --- | --- |
| `backend/app/work_schedule.py` | **ไฟล์ใหม่** — ตรรกะตัดสินสาย/ออกก่อนทั้งหมด |
| `backend/app/config.py` | พิกัด default ใหม่ + ตัวแปรเวลาทำงาน + `work_schedule_dict` |
| `backend/app/routers/checkins.py` | ต่อผลตัดสินเข้าข้อความ LINE (ใน `notify_checkin`) |
| `backend/app/routers/reports.py` | ส่ง `work_schedule` ออกทาง `/reports/geofence` |
| `backend/app/schemas.py` | เพิ่ม `WorkScheduleInfo` + ฟิลด์ใน `GeofenceInfo` |
| `backend/seed.py` | พิกัดตัวอย่างเป็นออฟฟิศใหม่ |
| `.env.example` | ตัวอย่าง `OFFICES` + เวลาทำงาน |

### Flutter (แก้เหมือนกันทั้ง 2 แอป)

| ไฟล์ | สิ่งที่ทำ |
| --- | --- |
| `lib/services/work_schedule.dart` | **ไฟล์ใหม่** — ตรรกะฝั่งแอป (ชุดเดียวกับ backend) |
| `lib/config.dart` | สถานที่เหลือแห่งเดียว + คลาส `WorkSchedule` |
| `lib/services/api_service.dart` | อ่าน `work_schedule` จาก `/reports/geofence` |
| `lib/services/location_service.dart` | เพิ่ม `setOfficesForTest` (ใช้ในเทสต์เท่านั้น) |
| `lib/widgets/today_attendance_card.dart` | ป้ายสาย/ตรงเวลา + สถานะรายบรรทัด |
| `lib/screens/tabs/checkin_tab.dart` | SnackBar บอกผลทันทีที่ลงเวลา |
| `test/work_schedule_test.dart` | **ไฟล์ใหม่** — 13 เทสต์ |

### ข้อมูล Google Takeout

| ไฟล์ | สิ่งที่ทำ |
| --- | --- |
| `Takeout/Maps/สถานที่ที่ติดป้ายกำกับของฉัน/สถานที่ที่ติดป้ายกำกับ.json` | เหลือหมุดเดียว = บริษัทใหม่ (จาก 28 หมุด) |

ไฟล์ Takeout อื่น (รีวิว, รูปภาพ, รายงานจราจร, การแก้ไขข้อมูลธุรกิจ) **ไม่แตะ**

---

## 6. ตัวอย่างข้อความ LINE ที่จะได้

**เข้างานสาย:**
```
สมชาย ได้ทำการเข้างานแล้ว (08:42 น. 10/09/2026)
ประเภทสถานที่: ที่ทำงาน
สถานที่ใกล้สุด: Motta & Montipa (Head office)
ระยะห่างจากที่ทำงาน: 120 เมตร
⚠️ สถานะ: สาย 12 นาที
เวลาทำงาน 08:30 - 17:30 น.
```

**เข้างานตรงเวลา:**
```
สมชาย ได้ทำการเข้างานแล้ว (08:20 น. 10/09/2026)
ประเภทสถานที่: ที่ทำงาน
สถานที่ใกล้สุด: Motta & Montipa (Head office)
ระยะห่างจากที่ทำงาน: 95 เมตร
✅ สถานะ: ตรงเวลา (ก่อนเวลา 10 นาที)
เวลาทำงาน 08:30 - 17:30 น.
```

---

## 7. ผลการทดสอบ

| รายการ | ผล |
| --- | --- |
| `flutter analyze` (flutter_app) | ✅ No issues found |
| `flutter analyze` (flutter_boss_app) | ✅ No issues found |
| `flutter test` (flutter_app) | ✅ 81 ผ่านทั้งหมด |
| `flutter test` (flutter_boss_app) | ✅ 120 ผ่านทั้งหมด |
| ตรรกะ backend (`work_schedule.py`) | ✅ ตรวจครบทุกเคสในตารางหัวข้อ 4 |
| `/reports/geofence` payload | ✅ ตรงตามหัวข้อ 3.3 |

**เทสต์เดิม 2 ตัวที่ต้องแก้:** `widget_test.dart` เทสต์เรื่อง "บ้านไม่นับเป็นที่ทำงาน"
เคยอาศัยพิกัดบ้านใน `Config.offices` — พอเหลือสาขาเดียวเลยพัง
แก้โดยให้เทสต์สร้างชุดสถานที่ของตัวเอง (`setOfficesForTest`) **ตรรกะไม่ได้ถูกแก้**
เทสต์จะได้ไม่พังอีกทุกครั้งที่บริษัทย้ายที่

---

## 8. ถ้าจะย้อนกลับ (rollback)

ตั้งใน `.env` แล้วรีสตาร์ท — ไม่ต้องแตะโค้ด ไม่ต้อง build แอป:

```env
# กลับไปใช้สถานที่เดิม
OFFICES=[{"name":"MARDODI","lat":13.9231953,"lng":100.5195808,"radius_km":2.0,"category":"work"},{"name":"BJH Bangkok","lat":13.8918358,"lng":100.563443,"radius_km":1.0,"category":"hospital"},{"name":"ถึงบ้านแล้ว","lat":13.8865664,"lng":100.5066278,"radius_km":0.2,"allow_checkout":false,"category":"home"}]

# ปิดการแจ้งเตือนสาย
ATTENDANCE_RULES_ENABLED=false
```

---

## 9. ที่ยังไม่ได้ทำ (เผื่อคุยกันต่อ)

- **หน้าเว็บ (frontend)** ยังไม่มีป้ายสาย/ตรงเวลา — งานนี้แก้เฉพาะ backend + แอป
- **รายงานสรุปรายเดือน** ยังไม่มีคอลัมน์ "มาสายกี่วัน"
- **ตารางวันหยุด/วันลา** ยังไม่มี (ดูข้อจำกัดหัวข้อ 4.4)
- **ที่อยู่แบบตัวอักษร** ของออฟฟิศใหม่ ยังว่างอยู่ (ดูหัวข้อ 2)
