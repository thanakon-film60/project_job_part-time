# Python
__pycache__/
*.pyc
venv/
.env
.env.bak
*.db

# Cloudflare Tunnel — ห้าม commit credentials เด็ดขาด
*.pem
.cloudflared/
cloudflared.log
# ไฟล์ credentials ของ tunnel (ชื่อเป็น <tunnel-id>.json)
1d37443e-8845-4c41-8c80-aa0f671ba280.json
backend/storage/*
!backend/storage/.gitkeep

# Node
node_modules/
dist/
dist_verify/
frontend/.env

# Flutter
flutter_app/.dart_tool/
flutter_app/build/
flutter_app/android/
flutter_app/ios/
flutter_app/.flutter-plugins*
# ไฟล์ติดตั้งที่ build มาจากเครื่อง dev — ตัวจริงอยู่ที่ backend/storage/app
# (ใหญ่ ~70 MB ห้ามให้หลุดขึ้น git) วางด้วย deploy\windows-server\publish-apk.ps1
*.apk
