# แอปเช็คอิน THANAKON-ROOM (Flutter)

แอป Android สำหรับพนักงาน — เช็คอินด้วย GPS + สแกนใบหน้า
คุยกับ backend ตัวเดียวกับเว็บ (ตั้ง URL ที่ `lib/config.dart`)

## build เป็นไฟล์ APK ให้พนักงานโหลดจากเว็บ

รันบนเครื่องที่มี Flutter SDK + Android SDK (เครื่อง production ไม่ต้องมี — ดูหัวข้อถัดไป):

```powershell
.\deploy\windows-server\build-flutter-apk.ps1
```

สคริปต์จะทำให้ทั้งหมดนี้เอง:

1. `flutter create --platforms=android .` ถ้ายังไม่มีโฟลเดอร์ `android/`
   (โฟลเดอร์นี้ไม่ได้ commit ลง git เพราะสร้างใหม่ได้)
2. เติมสิทธิ์กล้อง/ตำแหน่ง/แจ้งเตือน และตั้งชื่อแอปลง `AndroidManifest.xml`
   ตาม [PLATFORM_SETUP.md](PLATFORM_SETUP.md)
3. ตั้ง `minSdk = 23` (ML Kit + background service ต้องการ)
4. สร้างไอคอนแอปจาก **โลโก้ตัวเดียวกับเว็บ** (ดูหัวข้อถัดไป)
5. `flutter build apk --release`
6. copy ไฟล์ไปที่ `backend/storage/app/thanakon-checkin.apk`
   → พนักงานเห็นปุ่มดาวน์โหลดที่หน้าแรกของเว็บทันที ไม่ต้อง deploy เว็บใหม่

หรือสั่งพร้อม deploy เว็บในทีเดียว: `.\BUILD_DEPLOY.ps1 -BuildApk`

## ส่ง APK ขึ้นเครื่อง production

เครื่อง production ไม่มี Flutter SDK และ APK ก็ไม่ได้ commit ลง git (ไฟล์ 70+ MB)
จึงต้อง copy ไฟล์ `.apk` ไปเอง แล้วสั่งบนเซิร์ฟเวอร์:

```powershell
.\deploy\windows-server\publish-apk.ps1 -ApkPath D:\thanakon-checkin.apk
```

สคริปต์จะตรวจว่าไฟล์เป็น APK จริง วางลง `backend/storage/app/` แล้วเขียนเวอร์ชัน/วันที่ build
ลง `release.json` ให้หน้าเว็บ — ไม่ต้อง restart backend และไม่ต้อง deploy เว็บใหม่
เช็กผลที่ `https://thanakronpart-time.com/app/info` (ต้องได้ `"available": true`)

## ไอคอนแอป = โลโก้เดียวกับเว็บ

| ไฟล์ | ที่มา |
| --- | --- |
| `assets/icon/app-icon.png` | `frontend/public/logo-checkin.png` วางบนพื้นขาว |
| `assets/icon/app-icon-foreground.png` | โลโก้เดิมย่อให้อยู่ในโซนปลอดภัยของ adaptive icon (Android 8+) |

ตั้งค่าอยู่ในหัวข้อ `flutter_launcher_icons` ท้าย `pubspec.yaml`
ถ้าเปลี่ยนโลโก้ของเว็บ ให้สร้างสองไฟล์นี้ใหม่จากโลโก้ตัวใหม่ แล้ว build APK อีกครั้ง
(สคริปต์เรียก `flutter pub run flutter_launcher_icons` ให้อยู่แล้ว)

> ในไฟล์ตั้งค่าเปิดเฉพาะ `android: true` — `ios: false` เพราะ repo นี้ไม่มีโฟลเดอร์ `ios/`
> ถ้าเปิดไว้ flutter_launcher_icons จะล้มทั้งขั้นตอนตอนหาไฟล์ `Icon-App-*.png` ไม่เจอ
> (จะทำ iOS เมื่อไหร่ ให้ `flutter create --platforms=ios .` ก่อน แล้วค่อยสลับเป็น true)

## โครงหน้าจอ: Sidebar + แท็บ

หลังล็อกอินทุกอย่างอยู่ใน `AppShell` ([lib/screens/app_shell.dart](lib/screens/app_shell.dart))
เมนูด้านข้างเป็น **Drawer** บนมือถือ และ **ตรึงไว้ข้างจอ** อัตโนมัติเมื่อจอกว้าง ≥ 900 dp
(แท็บเล็ต/แนวนอน) — ใช้เนื้อหาเมนูชุดเดียวกันทั้งสองแบบ

| แท็บ | ไฟล์ | เห็นได้ | เนื้อหา |
| --- | --- | --- | --- |
| เช็คอินเข้างาน | `screens/tabs/checkin_tab.dart` | ทุกคน | ตำแหน่งปัจจุบัน · สถานะการติดตาม · ลงเวลาวันนี้ · ปุ่มเข้า/ออกงาน |
| ปฏิทินทีม | `screens/tabs/team_tab.dart` | หัวหน้า | ปฏิทินรายเดือน ใครลงเวลาวันไหน กดวันเพื่อดูเวลาเข้า–ออกรายคน |
| ข้อมูลพนักงาน | `screens/tabs/employees_tab.dart` | หัวหน้า | รายชื่อ + ค้นหา · ทางเข้าแฟ้มพนักงาน · ลงทะเบียนพนักงานใหม่ |
| แผนที่ติดตาม | `screens/tabs/live_map_tab.dart` | หัวหน้า | ตำแหน่งล่าสุดของทุกคนบนแผนที่ · วงเขต · เส้นทางย้อนหลัง |
| ประวัติการลงเวลา | `screens/tabs/history_tab.dart` | ทุกคน | ย้อนหลัง 30 วัน กดกางดูรายการของแต่ละวัน |
| บัญชีของฉัน | `screens/tabs/profile_tab.dart` | ทุกคน | ข้อมูลผู้ใช้ · ประวัติใบหน้า + บันทึกรูปใหม่ · เวอร์ชันแอป |
| สถานที่ & สถานะระบบ | `screens/tabs/places_tab.dart` | ทุกคน | รัศมีที่เช็คอินได้ + ระยะห่างตอนนี้ · เหลือเวลาใช้งานอีกเท่าไร |

แท็บของหัวหน้าโผล่เฉพาะบัญชีที่ `is_manager = true` ซึ่งมาจาก response ของ
`POST /auth/login` (แอปเก็บไว้ที่ `ApiService.account`) — **การซ่อนเมนูเป็นแค่ความสะดวก
ตัวกันจริงคือ `require_manager` ที่ backend** ซึ่งกันทุก endpoint ของหัวหน้าไว้อยู่แล้ว

### เพิ่มแท็บใหม่

1. สร้างไฟล์หน้าจอใน `lib/screens/tabs/` (คืน widget ธรรมดา ไม่ต้องมี `Scaffold`/`AppBar` — shell จัดให้)
2. เพิ่ม `AppTab` หนึ่งตัวใน [lib/navigation/app_tabs.dart](lib/navigation/app_tabs.dart)

```dart
AppTab(
  id: 'leave',
  label: 'ขอลา',
  subtitle: 'ยื่นใบลา / ดูสถานะ',
  icon: Icons.event_busy,
  builder: (_) => const LeaveTab(),
),
```

เท่านี้เมนูขึ้นเอง ทั้งบน Drawer และ sidebar แบบตรึง ไม่ต้องแก้ `AppShell` เลย
ลำดับในลิสต์ = ลำดับบนเมนู

> แท็บถูกเก็บไว้ใน `IndexedStack` — สลับไปมาแล้วสถานะเดิม (ตำแหน่งที่เลื่อน, ข้อมูลที่โหลดไว้)
> ยังอยู่ครบ ไม่ต้องโหลดใหม่ทุกครั้งที่เปลี่ยนแท็บ

### ของที่ต้องเดินตลอด ไม่ผูกกับแท็บ

`AppShell` ถือไว้เองทั้งหมด แท็บจึงเหลือแค่หน้าที่แสดงผล:

- **การติดตามตำแหน่ง** — `TrackingController` ([lib/services/tracking_controller.dart](lib/services/tracking_controller.dart))
  เป็น `ChangeNotifier` ตัวเดียวของทั้งแอป แท็บไหนอยากรู้สถานะก็ `addListener` เอา
- **การทวงสิทธิ์ "อนุญาตตลอดเวลา"** ทุก 10 นาที
- **การเด้งออกจากระบบตอน 4 ทุ่ม**

## เมนูของหัวหน้า (พอร์ตมาจากเว็บ)

ก่อนหน้านี้งานของหัวหน้าทำได้เฉพาะบนเว็บ ตอนนี้ย้ายมาอยู่ในแอปครบแล้ว
โดยยิงไปที่ endpoint ชุดเดียวกับที่หน้าเว็บใช้ ไม่มีการเพิ่ม API ใหม่ที่ backend

| งาน | หน้าจอในแอป | เทียบกับเว็บ | API |
| --- | --- | --- | --- |
| ปฏิทินทีมรายเดือน | `tabs/team_tab.dart` | `pages/DashboardPage.jsx` | `GET /reports/team-calendar` |
| รายชื่อพนักงาน + ค้นหา | `tabs/employees_tab.dart` | `pages/EmployeesPage.jsx` | `GET /reports/employees` |
| แฟ้มพนักงานรายคน | `screens/employee_detail_screen.dart` | `pages/EmployeeHistoryPage.jsx` | `GET /reports/employees/{id}/history` |
| แก้ไขข้อมูลพนักงาน | `screens/employee_edit_screen.dart` | `components/EmployeeEditDialog.jsx` | `PATCH /employee-management/{id}` |
| ลงทะเบียนพนักงานใหม่ | `screens/employee_register_screen.dart` | `pages/EmployeeRegistrationPage.jsx` | `POST /employee-management` |
| แผนที่ติดตาม | `tabs/live_map_tab.dart` | `pages/LiveMapPage.jsx` | `GET /locations/live`, `GET /locations/trail/{id}` |

ตรรกะที่ต้องตรงกันทั้งสามฝั่ง (backend / เว็บ / แอป) ถูกพอร์ตมาเป็น Dart ล้วน
พร้อม unit test ใน `test/employee_logic_test.dart`:

| ตรรกะ | ไฟล์ในแอป | ต้นทาง |
| --- | --- | --- |
| checksum เลขบัตรประชาชนไทย | `services/thai_id.dart` | `backend/app/schemas.py` · `frontend/src/lib/thai-id.js` |
| กฎตรวจฟอร์มลงทะเบียน + payload | `services/employee_registration.dart` | `frontend/src/lib/employee-registration.js` |
| ข้อความ "อยู่บ้าน / ในเขต / นอกเขต" | `models/live_location.dart` | `frontend/src/lib/attendance.js` |
| การตีความเวลาที่ไม่มี timezone | `models/json.dart` | `frontend/src/lib/attendance.js` (`thaiFrom`) |

> ฟอร์มลงทะเบียนแบ่ง 3 ขั้นเหมือนเว็บ และ **กดถัดไปไม่ได้จนกว่าผลค้นรหัสไปรษณีย์
> จะกลับมา** — ไม่งั้นจะไปเจอ 422 ตอนกดบันทึกจริง เพราะ backend บังคับว่าที่อยู่
> ต้องตรงกับฐานข้อมูลรหัสไปรษณีย์ และแผนก/ตำแหน่งต้องมีอยู่ใน `employment_options` ก่อน
>
> การแก้ไขแฟ้มพนักงานส่งเฉพาะช่องที่ **เปลี่ยนจริง** (`PATCH` ใช้ `exclude_unset`)
> ไม่งั้น Timeline แฟ้มพนักงานจะมีรายการ "แก้ไขข้อมูล" ทั้งที่ไม่ได้แก้อะไรเลย

## ประวัติใบหน้า (บันทึกจากมือถือได้แล้ว)

แท็บ "บัญชีของฉัน" แสดงรูปยืนยันตัวตนของตัวเองทั้งหมด และบันทึกรูปใหม่ได้จากในแอป
(`screens/face_enroll_screen.dart` → `POST /faces/enroll` พร้อม `source=mobile`)

ต่างจากการลงเวลาตรงที่ **ไม่ผูกกับตำแหน่ง** — เป็นภาพอ้างอิงไว้เทียบตัวตนภายหลัง
จึงบันทึกจากที่ไหนก็ได้ ไม่ต้องอยู่ในเขต

เดิมทำได้เฉพาะบนเว็บ ซึ่งต้องเปิดผ่าน HTTPS และอนุญาตกล้องของเบราว์เซอร์ก่อน
ส่วนในแอปใช้กล้องหน้าตัวเดียวกับตอนลงเวลา

> ไฟล์รูปถูกกันด้วย JWT (`GET /faces/{id}/photo` — เจ้าของหรือหัวหน้าเท่านั้น)
> จึงใช้ `Image.network` ตรง ๆ ไม่ได้ ต้องดึงเป็น bytes เองแล้ววาดด้วย `Image.memory`
> — อยู่ใน `widgets/face_photo.dart` ซึ่ง cache ไว้ในหน่วยความจำและ **ล้างทิ้งตอนออกจากระบบ**
> เครื่องที่ใช้ร่วมกันจะได้ไม่เห็นรูปของคนก่อนหน้า

### จัดลำดับเอง (ลากสลับ) และลบรูปที่ไม่ต้องการ

**ลำดับมีความหมาย**: รูปแรกในแกลเลอรีคือ *รูปประจำตัว* ที่ระบบเอาไปแสดงทุกที่ —
หัวเมนูในแอป รายชื่อพนักงาน และหน้าแฟ้มพนักงานของหัวหน้า พนักงานจึงเลือกเองได้ว่า
จะให้คนอื่นเห็นรูปไหน ด้วยการกดค้างแล้วลากรูปนั้นมาไว้อันแรก (ใบแรกมีกรอบสีและ
ป้าย ★ รูปประจำตัว กำกับ)

| การกระทำ | API | ใครทำได้ |
| --- | --- | --- |
| ลากสลับลำดับ | `PUT /faces/order` | เจ้าของเท่านั้น — หัวหน้าเลือกรูปประจำตัวแทนคนอื่นไม่ได้ |
| ลบรูป | `DELETE /faces/{id}` | เจ้าของหรือหัวหน้า (กฎเดียวกับสิทธิ์การดูรูป) |

`widgets/face_gallery.dart` เขียน drag & drop เองด้วย `LongPressDraggable` + `DragTarget`
แทนที่จะเพิ่มแพ็กเกจ reorderable grid — Flutter มีให้แต่ `ReorderableListView` ที่เป็น
ลิสต์แนวตั้ง ส่วนรูปต้องเรียงเป็นกริด และตรรกะที่ต้องใช้จริงมีแค่ "ย้ายจากตำแหน่ง A ไป B"

พฤติกรรมสำคัญที่มี unit test ครอบไว้ใน `test/face_gallery_test.dart`:

- **ขยับทันทีที่ปล่อยนิ้ว** ไม่รอเซิร์ฟเวอร์ตอบ แล้ว **ย้อนกลับลำดับเดิมถ้าบันทึกไม่สำเร็จ**
  พร้อมขึ้นข้อความบอกสาเหตุ — ผู้ใช้ต้องไม่เห็นลำดับที่เซิร์ฟเวอร์ไม่ได้รับไว้
- ลากแล้ววางที่เดิม = ไม่ยิง API
- ห้ามลากซ้อนระหว่างที่รอบก่อนยังบันทึกไม่เสร็จ (`maxSimultaneousDrags: 0`)
  ไม่งั้นคำสั่งสองรอบจะไปถึงเซิร์ฟเวอร์คนละลำดับ
- ลบต้องยืนยันก่อนเสมอ และรูปหายจากจอต่อเมื่อเซิร์ฟเวอร์ตอบสำเร็จ

> `PUT /faces/order` บังคับให้ส่ง id ของรูป **ทุกใบ** มาพร้อมกัน ถ้าส่งไม่ครบ backend
> ตอบ 400 — กันไม่ให้เหลือรูปที่ `sort_order` ค้างครึ่ง ๆ กลาง ๆ แล้วตำแหน่งเพี้ยน
> ในการจัดครั้งถัดไป ส่วนรูปที่ยังไม่เคยถูกจัดลำดับ (`sort_order` = NULL) จะต่อท้าย
> โดยเรียงตามเวลาบันทึกใหม่สุดขึ้นก่อน = พฤติกรรมเดิมก่อนมีฟีเจอร์นี้

> การลบแตะเฉพาะตาราง `face_profiles` ซึ่งเป็นรูป **อ้างอิง** ที่พนักงานถ่ายเก็บไว้เอง
> ไม่เกี่ยวกับ `checkins.photo_path` ที่เป็นรูปหลักฐานของการลงเวลาแต่ละครั้ง
> พนักงานจึงลบประวัติการเข้างานของตัวเองผ่านทางนี้ไม่ได้

## กล้อง + liveness ใช้ตัวเดียวกันทั้งสองงาน

`widgets/face_scanner.dart` ถือเรื่องกล้องกับ ML Kit ไว้ทั้งหมด ทั้งหน้าลงเวลา
(`screens/checkin_screen.dart`) และหน้าบันทึกใบหน้าใช้ตัวนี้ร่วมกัน ต่างกันแค่
"ทำอะไรกับรูปที่ถ่ายได้" ซึ่งส่งเข้าไปเป็น callback

callback คืน `null` = สำเร็จ, คืนข้อความ = ไม่สำเร็จ แล้ว scanner จะขึ้นข้อความนั้น
เป็นคำแนะนำและเปิดสตรีมกล้องต่อให้ลองใหม่ (หน้าลงเวลาใช้ช่องทางนี้บอกว่าอยู่นอกเขต)

## รายการลงเวลาของวันนี้ (หน้าแรก)

หลังล็อกอิน หน้าแรกไม่ได้บอกแค่พิกัดกับระยะห่างแล้ว แต่มีการ์ด **"การลงเวลาวันนี้"**
ที่ดึงจาก `GET /checkins/me?days=1` มาแสดงเป็นรายการ (List) ของวันนี้ตามเวลาไทย

| ส่วนที่แสดง | รายละเอียด |
| --- | --- |
| สรุปด้านบน | เข้างานครั้งแรกกี่โมง · ออกงานล่าสุดกี่โมง · รวมเวลาทำงาน (เดินสดทุก 30 วินาที) |
| รายการแต่ละแถว | เข้างาน/ออกงาน · เวลา · ชื่อสถานที่ · ระยะห่าง · ป้าย "นอกเขต" ถ้าอยู่นอกรัศมี |
| สถานะ | "กำลังทำงานอยู่" เมื่อกดเข้างานแล้วยังไม่ได้กดออกงาน |

การจับคู่เข้า-ออกอยู่ใน `lib/services/attendance_service.dart` (มี unit test ครอบใน `test/widget_test.dart`)
เผื่อเคสที่เกิดจริง: กดเข้างานซ้ำ (ยึดครั้งแรก), มีออกงานลอยๆ ไม่มีเข้างานคู่กัน (โชว์ในรายการ แต่ไม่นับเวลา)

รีเฟรชอัตโนมัติทุก 2 นาที + ทุกครั้งที่กลับเข้าแอป + ทันทีหลังลงเวลาสำเร็จ (ดึงหน้าจอลงก็รีเฟรชได้)

> `days` / `limit` เป็นพารามิเตอร์ใหม่ของ backend ถ้าเซิร์ฟเวอร์ยังไม่อัปเดต
> จะคืนประวัติทั้งหมดมาตามเดิม — แอปกรองเฉพาะวันนี้ให้อยู่แล้ว จึงยังทำงานได้ปกติ

## อยู่บ้าน = ไม่ได้ไปทำงาน (ไม่มีออกงาน)

สถานที่ที่ถูกจัดเป็น "บ้าน" (`category: "home"` ใน OFFICES หรือชื่อมีคำว่า บ้าน/home/house)
ไม่ถือเป็นการมาทำงาน กติกาทั้งระบบจึงเป็นแบบนี้:

| สถานการณ์ | ผลลัพธ์ |
| --- | --- |
| อยู่ในเขตบ้าน | หน้าแรกขึ้น "อยู่บ้าน — ไม่ได้ไปทำงาน" ไม่มีปุ่มเข้างาน/ออกงาน |
| กดปุ่มเดียวที่มี | "บันทึกว่าอยู่บ้าน" — ไว้ให้หัวหน้ารู้ว่าวันนี้อยู่บ้าน **ไม่ต้องกดออกงานตามมา** |
| บันทึกว่าอยู่บ้านแล้ววันนี้ | ปุ่มเปลี่ยนเป็น "บันทึกว่าอยู่บ้านแล้วเมื่อ HH:mm น." กดซ้ำไม่ได้ |
| วันที่มีแต่รายการที่บ้าน | สรุปวันนั้นขึ้น "อยู่บ้าน — ไม่ได้ไปทำงาน" รวมเวลาทำงาน `-` และไม่เตือนว่าลืมกดออกงาน |
| เข้างานที่ที่ทำงานไว้ แล้วกลับบ้านโดยไม่ได้กดออก | หน้าแรกเตือนว่ายังค้างการเข้างานอยู่ ต้องไปกดออกงานที่ที่ทำงาน (กดที่บ้านไม่ได้) |

ตรรกะการจับคู่เวลาทำงานอยู่ที่ `DayAttendance._pair` — **กรองรายการที่บ้านออกก่อนเสมอ**
รายการที่บ้านยังโชว์ในลิสต์ว่า "อยู่บ้านแล้ว" (ไอคอนบ้าน สีม่วง) แต่ไม่เปิดช่วงเวลาทำงาน จึงไม่มีวัน
กลายเป็น "ยังไม่ได้กดออกงาน" และไม่ถูกนับเป็นชั่วโมงทำงาน

> ก่อนหน้านี้การกด "ถึงบ้านแล้ว" ถูกนับเป็นเข้างาน ทำให้เวลาทำงานเดินทั้งวันและ
> ขึ้นเตือนว่าลืมกดออกงานทุกวัน — เงื่อนไขนี้แก้ตรงนั้น

> พนักงานยัง **ต้องเข้าสู่ระบบทุกวัน** ถึงจะรู้ว่าใครอยู่ที่ไหน — การอยู่บ้าน
> ไม่ได้แปลว่าไม่ต้องเปิดแอป

ฝั่ง backend และเว็บยึดกติกาเดียวกัน: `/checkins` ปฏิเสธการออกงานที่บ้านอยู่แล้ว
(`work_only` ตัดสถานที่หมวด home ทิ้ง), แจ้ง LINE ว่า "อยู่บ้านแล้ว" แทน "เข้างาน",
สรุปประจำวันแยกคนที่อยู่บ้านออกจากคนที่มาทำงาน, ปฏิทิน (`/reports/calendar`,
`/reports/team-calendar`) ไม่เอาการลงเวลาที่บ้านมาเป็นเวลาเข้า/ออกงาน และส่งฟิลด์
`home_only` ให้เว็บขึ้นป้าย "อยู่บ้าน — ไม่ได้ไปทำงาน" 

## ติดตามตำแหน่งตั้งแต่ล็อกอิน จนกว่าจะออกจากระบบ

การส่งพิกัดเริ่มทันทีที่เข้าหน้าแรก **ไม่ต้องรอกดเข้างาน และไม่สนใจว่าอยู่ในเขตหรือไม่** —
อยู่บ้านแต่ล็อกอินค้างไว้ก็ยังส่งพิกัดทุก 60 วินาที (`Config.pingIntervalSeconds`)
หยุดเมื่อ **ออกจากระบบ** หรือ **ถึง 4 ทุ่ม** เท่านั้น

| เงื่อนไข | พฤติกรรม |
| --- | --- |
| ยังไม่ได้สิทธิ์ "อนุญาตตลอดเวลา" | หน้าแรกขึ้นแถบสีส้มค้างไว้ + ทวงซ้ำทุก 10 นาที (`locationPermissionNagInterval`) จนกว่าจะได้หรือออกจากระบบ |
| Android 11+ | เลือก "อนุญาตตลอดเวลา" จากป๊อปอัปไม่ได้ ระบบบังคับให้ไปกดในหน้าตั้งค่า — ปุ่ม "เปิดหน้าตั้งค่า" พาไปให้ |
| ปิด GPS ทั้งเครื่อง | แถบเปลี่ยนเป็นสีแดง พร้อมปุ่มเปิดหน้าตั้งค่าตำแหน่ง |
| ไม่ได้สิทธิ์แจ้งเตือน | ยังติดตามต่อ เพียงแต่ไม่เห็นแถบ "กำลังติดตามตำแหน่ง" (ของเดิมยกเลิกการติดตามทั้งหมด) |
| ปิดแอป/สลับไปแอปอื่น | foreground service (ชนิด `location`) ยังส่งต่อ และอัปเดตเวลาส่งล่าสุดบนแถบแจ้งเตือน |
| เปิดเครื่องใหม่ | `autoStartOnBoot` เริ่ม service ให้เอง ถ้ายังล็อกอินอยู่ ถ้าไม่ใช่ก็หยุดตัวเองในรอบแรก |
| ระบบประหยัดแบตของเครื่อง | ขอยกเว้น (`REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`) หนึ่งครั้งตอนเริ่มติดตาม ไม่ให้ก็ยังทำงาน แต่พิกัดอาจขาดช่วง |

isolate เบื้องหลังอ่าน token ใหม่ทุกรอบ (`ApiService.loadToken`) จึงรู้ทันทีเมื่อมีการล็อกอินใหม่
หรือถูกเด้งออก แล้ว `stopSelf()` ตัวเองเมื่อไม่มี session เหลือ

## หมดเวลาใช้งานประจำวัน (4 ทุ่ม)

ทุกเครื่องจะถูกเด้งออกจากระบบตอน **22:00 น. เวลาไทย** แล้วต้องล็อกอินใหม่ในวันถัดไป
กันเคสลืมออกจากระบบแล้วเครื่องส่งพิกัด GPS ต่อทั้งคืน และกัน token ค้างในเครื่องที่ทำหาย

| ตอนนั้นแอปอยู่สถานะไหน | เกิดอะไรขึ้น |
| --- | --- |
| เปิดหน้าจอค้างไว้ | Timer ครบเวลา → หยุด background service → กลับไปหน้าล็อกอินพร้อมข้อความบอกเหตุผล |
| ถูกพักไว้เบื้องหลัง / เพิ่งเปิดขึ้นมาใหม่ | เช็กเวลาซ้ำตอน resume และตอนเปิดแอป (`ApiService.loadToken`) |
| ส่งพิกัดอยู่ใน background isolate | `sendPing` เรียก `ensureSession()` ก่อนทุกครั้ง เลยเวลาแล้วจะหยุดส่งเอง |

ตั้งค่าเวลาได้ที่ `lib/config.dart` → `sessionEndHour` / `sessionEndMinute`
(ยึดเวลาไทยด้วย `thaiUtcOffset` ไม่ใช่ timezone ของเครื่อง — มือถือที่ตั้งเวลาผิดจะได้หมดพร้อมกัน)

> เวอร์ชันแรกที่มีเงื่อนไขนี้คือ 1.1.0 — เครื่องที่อัปเดตจากตัวเก่าจะโดนให้ล็อกอินใหม่หนึ่งครั้ง
> เพราะ token เดิมไม่มีเวลาหมดอายุติดมาด้วย

> ฝั่ง backend ยังออก JWT อายุ 12 ชม. ตามเดิม (`access_token_expire_minutes`)
> เงื่อนไข 4 ทุ่มนี้บังคับที่ตัวแอป ถ้าต้องการให้ตัว token หมดอายุจริงตาม ต้องแก้ที่ backend ด้วย

## แผนที่: flutter_map + การตรึงเวอร์ชัน path_provider_android

หน้าแผนที่ติดตามใช้ `flutter_map` + `latlong2` (Dart ล้วน ไม่มี native SDK ให้ตั้งค่า
และไม่ต้องใช้ API key เหมือน Google Maps) วาด tile จาก OpenStreetMap
โดยส่ง `userAgentPackageName` ไปตาม usage policy ของ OSM

`flutter_map` ดึง `path_provider` มาด้วย (ไว้หาที่เก็บ cache ของ tile) และ
**ต้องตรึง `path_provider_android` ไว้ที่ 2.2.23** ผ่าน `dependency_overrides`
ใน `pubspec.yaml` เพราะ 2.3.x เปลี่ยนไปเรียก native ผ่าน package `jni`
ซึ่ง build ไม่ผ่านกับ Gradle/AGP ชุดที่โปรเจ็กต์นี้ใช้อยู่:

```
Could not determine the dependencies of task ':jni_flutter:compileReleaseJavaWithJavac'
> Cannot query the value of this provider because it has no value available.
```

ถอดบรรทัดที่ตรึงไว้ออกได้เมื่ออัปเกรด AGP/Gradle แล้วลอง `flutter build apk` ผ่าน

## รันตอนพัฒนา

```powershell
flutter pub get
flutter run
```

### build/ติดตั้งลงเครื่องจริงเครื่องเดียว

มือถือ Android ปัจจุบันเป็น arm64 เกือบทั้งหมด ระบุ target platform ไว้จะได้ APK
เล็กลงและ build เร็วขึ้นมาก (ไม่ต้องรวมโค้ด armeabi-v7a/x86_64 ที่เครื่องนั้นไม่ใช้):

```powershell
flutter devices                                             # ดูรหัสเครื่อง
flutter build apk --release --target-platform android-arm64
flutter install --release -d <device-id>
```

> APK ที่แจกให้พนักงานผ่านหน้าเว็บต้อง build แบบ **ไม่ระบุ** `--target-platform`
> (ใช้ `deploy\windows-server\build-flutter-apk.ps1` ตามหัวข้อบนสุด) เพราะไฟล์เดียว
> ต้องติดตั้งได้กับมือถือทุกรุ่นในทีม รวมถึงเครื่องเก่าที่ยังเป็น 32 บิต
